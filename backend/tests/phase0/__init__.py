"""V5 Phase 0 tests (docs/v5/01_PHASE_0_canonical_truth.md TESTS section).

A package (not a bare directory) so `tests.phase0.fixtures` is importable
from every module here without path games -- the rest of the suite is flat
and does not need this, Phase 0 does because its fixture signal sets are
shared across six test modules.
"""
