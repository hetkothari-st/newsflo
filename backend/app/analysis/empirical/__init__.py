"""V5 PHASE 3 -- the reverse event study.

The §10 event-study machinery was specified as a CHECKER: did the candidate
we published actually move? Run it backwards over the whole listed universe
and it becomes a BLIND-SPOT DETECTOR: which industries move that we cannot
explain at all?

Its output is a work queue (`coverage_gap`), never a publication. See
`gap_finder.py`.
"""
