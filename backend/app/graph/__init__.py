"""V5 PHASE 3 -- the causal graph.

`mechanism_edge` rows plus the walk over them (`traverse.py`), and the
input-output bootstrap that PROPOSES edges for human review
(`io_bootstrap/`). An edge proposed by a table is not an edge until a person
has said so.
"""
